---
apiVersion: v1
kind: Service
metadata:
  name: podtato-head-left-arm
  labels:
    app.kubernetes.io/name: podtato-head
    app.kubernetes.io/component: left-arm
spec:
  type: ClusterIP
  ports:
    - port: 9003
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: podtato-head
    app.kubernetes.io/component: left-arm
