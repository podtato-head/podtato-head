---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: podtato-head-right-leg
  labels:
    app.kubernetes.io/name: podtato-head
    app.kubernetes.io/component: right-leg
    app.kubernetes.io/version: {{ podtato.version }}
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: {{ replicas.right_leg }}
  selector:
    matchLabels:
      app.kubernetes.io/name: podtato-head
      app.kubernetes.io/component: right-leg
  template:
    metadata:
      labels:
        app.kubernetes.io/name: podtato-head
        app.kubernetes.io/component: right-leg
    spec:
      terminationGracePeriodSeconds: 5
      serviceAccountName: podtato-head
      containers:
        - name: podtato-head-right-leg
          image: ghcr.io/podtato-head/right-leg:{{ podtato.version }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: http
          readinessProbe:
            tcpSocket:
              port: http
          resources: {}
          env:
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
