---
apiVersion: v1
kind: Service
metadata:
  name: podtato-head-entry
  labels:
    app.kubernetes.io/name: podtato-head
    app.kubernetes.io/component: entry
spec:
  type: LoadBalancer
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: podtato-head
    app.kubernetes.io/component: entry